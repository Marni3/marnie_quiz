# Development Changelog — 2026-08-31

### 1. Robust KaTeX Prose Boundary Guard, Code Span Pills, and Dynamic "Continue Last Module" Fallback
- **KaTeX Inline Delimiter Sanity & Prose Protection (`math-text.tsx`)**:
  - Restrained inline math tokenization to single-line spans (`\$[^\$\n\r]+?\$`) so unmatched `$` delimiters cannot consume multi-line paragraphs.
  - Added prose-boundary heuristic to automatically detect and de-escalate LLM-generated math tokens that inadvertently wrap entire English sentences or markdown styling (`**bold**`).
  - Added inline code span rendering (`` `...` ``) with styled pill containers (`px-1.5 py-0.5 rounded-md bg-[var(--surface2)] font-mono text-xs text-[var(--accent)] font-semibold`) for calculator tricks and keystroke sequences like `` `[\sqrt{}]` ``.
- **Strict Prompt Math Delimiter Directives (`prompts.ts`)**:
  - Updated `BASE_SOCRATIC_PROMPT` with strict rules forbidding the wrapping of plain English sentences, phrases, or markdown inside `$...$` delimiters.
- **Dynamic "Continue Last Module" CTA Bar (`module-reader.tsx`)**:
  - Guaranteed that the post-drill momentum CTA will always display **`[ 📖 Continue Last Module: {CODE} ]`** (with dynamic fallback to the current module's domain code or `EST-01`) instead of the generic "Explore Module Library".
- **Production Build Verified**: `npm run build` compiled cleanly with 0 errors across all 27 Next.js routes.

### 3. Universal LaTeX/Math Pre-Normalizer & Calculator Keycap Pills
- **Universal Math Pre-Normalization (`math-text.tsx`)**:
  - Added `preNormalizeMathText` to automatically convert LaTeX-style delimiters `\( ... \)` and `\[ ... \]` to `$ ... $` and `$$ ... $$`.
  - Added normalization for escaped variables (e.g. `\Z_L\`, `\Z_{in}\`, `\lambda/4\`) to clean inline math `$Z_L$`, `$Z_{in}$`, `$\lambda/4$`.
  - Converted un-delimited math operators (`\times` -> `×`, `\sqrt{}` -> `√`, `\div` -> `÷`, `\pm` -> `±`) so raw backslashes never render as broken text.
- **Bracket Keycap Rendering (`math-text.tsx`)**:
  - Automatically renders bracketed calculator keys like `[ √ ]`, `[ = ]`, `[ MODE ]`, `[ STAT ]` inside styled `<kbd>` keycap pills with surface background and contrast borders.
### 4. Navbar Streamlining, Type-to-Confirm FSRS Reset & 3-Column Retention Board (`main`)
- **Navbar De-Cluttering (`navbar.tsx`)**:
  - Removed `Tour`, `Feedback`, and `ThemeToggle` from top navbar, moving them into `/settings`.
  - Replaced text `Search ⌘K` button with a compact search icon button triggering `open-omni-search`.
- **App Preferences & Type-to-Confirm Data Reset (`settings-view.tsx`, `storage.ts`)**:
  - Added App Preferences section with Theme mode toggle, Guided Tour launcher, and Feedback submission modal.
  - Implemented high-safety confirmation modal requiring users to type `"Reset my progress"` before enabling the permanent FSRS/data purge.
  - Updated `resetAllProgressData()` to wipe all FSRS stability intervals, attempt logs, streak history, notes, and cache keys.
- **3-Bucket Spaced Repetition Retention Board (`retention-board.tsx`, `analytics/page.tsx`)**:
  - Created 3-column Kanban layout on desktop and segmented tab switcher on mobile.
  - Automatically categorizes syllabus topics into:
    1. **🚨 Due for Review Today**: $R < 85\%$ or `nextReviewDue <= now`.
    2. **⏳ Upcoming (1–3 Days)**: $85\% \le R < 95\%$.
    3. **🏆 Mastered & Stable**: Stability $S \ge 14\text{ days}$ & Accuracy $\ge 85\%$.
  - Includes quick-action `[ ⚡ Practice ]` and `[ 📖 Study Notes ]` on every topic card.
- **Production Build Verified**: `npm run build` compiled with 0 errors across all 27 Next.js routes.

### 5. High-Fidelity "Ask AI" Context Bridge & Simplified Prompting (`main`)
- **Tutor Pending Context Protocol (`storage.ts`, `types.ts`)**:
  - Implemented `TutorPendingContext`, `setPendingTutorContext`, and `getAndClearPendingTutorContext` to transport structured lesson highlights, module metadata, and question debriefs directly into the AI workspace.
- **Module Reader Integration (`module-reader.tsx`)**:
  - Updated selection popover "Ask AI" button to package the highlighted excerpt, module code, and subtopic title with a simplified, friendly prompt:
    `"Could you explain this to me in a plain and easy to understand way then give me examples or practice to work with?"`
- **Tutor Workspace Ingestion & Prompt Injection (`tutor-view.tsx`, `prompts.ts`)**:
  - Ingests pending highlight context or search parameters on mount.
  - Automatically creates a dedicated chat session and mounts an **Attached Lesson Context / Quote Box** above the input composer.
  - Formats the exact highlight excerpt into the LLM system prompt for 100% conceptual alignment.
  - Auto-initiates streaming explanation when BYOK API keys are present.
- **Roadmap Expanded (`implementation_plan.md`)**:
  - Added Section 5 for Long-Term Features: Inline Marginalia / PDF-style Sidebar Notes and `@media print` Clean PDF Handout Export.
### 6. Quoted Highlight in Ask AI Prompt, Math Legacy Count Fix & Viral Referral Roadmap (`main`)
- **Quoted Highlight in User Prompt (`module-reader.tsx`)**:
  - Updated selection popover prompt generation to explicitly quote the highlighted text directly in the user message:
    `Regarding "${highlight}": Could you explain this to me in a plain and easy to understand way then give me examples or practice to work with?`
- **Math Legacy Module Count Analytics Fix (`analytics.ts`)**:
  - Filtered out `!m.isLegacy` in `getUserAnalyticsOverview` so that legacy Math modules are excluded from the subject syllabus total (correctly showing 32 active Math modules instead of 64).
- **Public Release Roadmap Additions (`implementation_plan.md`)**:
  - **Section 5.3 Viral Referral & Study Buddy Engine**: 50-item mock exam unlock for 2 buddy invites, pass-along pro tiers, and group study packs.
  - **Section 5.4 Bite-Sized / Sprint Micro-Module Architecture**: 3-to-5 minute bite-sized lessons with 1 mental anchor, 1 formula, 1 Karce calculator trick, and 3 rapid concept checks alongside comprehensive 25-page handouts.
- **Production Build Verified**: `npm run build` compiled with 0 errors across all 27 Next.js routes.

### 7. Server-Side Database Progress Reset API (`/api/user/reset`) (`main`)
- **Root-Cause Resolution**:
  - The previous reset only wiped client-side `localStorage`, which left server-side PostgreSQL records (`attempts`, `answer_records`, `user_topic_srs`, and `user_module_progress`) intact.
- **Server Reset Endpoint (`/api/user/reset/route.ts`)**:
  - Created `POST /api/user/reset` to delete all `userTopicSrs` rows, `userModuleProgress` records, and `attempts` (cascading to `answerRecords`) for both authenticated user sessions and default guest identities in PostgreSQL.
- **Settings Sync (`settings-view.tsx`)**:
  - Updated `handleResetAllProgress` to call `/api/user/reset`, wipe `localStorage`, clear study streaks, and trigger automatic UI reload.
### 8. Module & Test-Set Quality Validation Plan + MATH-01-01 Fixes (`main`)
- **Module & Test-Set Validation Plan (`module_testset_validation_plan.md`)**:
  - Published comprehensive 4-phase audit roadmap covering:
    1. Phase 1: ECE Laws & Ethics (`GEAS-10-01` to `GEAS-10-03` + 75-item mastery sets).
    2. Phase 2: Mathematics Learning Modules (32 active modules).
    3. Phase 3: Mathematics Mastery Challenges & CSV Question Banks.
    4. Phase 4: Automated CI Lint Script (`scripts/audit-modules.mjs`).
- **MATH-01-01 Concept Checks & Calculator Parity (`math-01-01.json`)**:
  - Balanced answer key distribution across B, C, A with rich individual `distractorReason` fields.
  - Synchronized `karce` and `canon` calculator guides to solve the **exact same representative problem** ($i^{79} + (1+i)^4$), translating keystrokes 1-to-1 to each keypad.
- **Distractor Explanation Fallback Guard (`module-reader.tsx`)**:
  - Updated `normalizedConceptChecks` so that choosing an incorrect distractor never blindly duplicates the correct answer's rationale.
### 9. Phase 1: ECE Laws & Ethics Complete Quality Remediation (`main`)
- **Automated CI Quality Linter (`scripts/audit-modules.mjs`, `package.json`)**:
  - Integrated `npm run audit` across all 46 active modules and 46 mastery challenges.
- **GEAS-10-02 & GEAS-10-03 Quality Upgrades (`geas-10-02.json`, `geas-10-03.json`)**:
  - Stripped hardcoded section numbers from TOC titles to match dynamic rendering specs.
  - Shuffled concept check answers across B, C, D, A with bespoke `distractorReason` fields deconstructing statutory board traps.
- **GEAS Mastery Challenges Balanced (`geas-10-01-mastery.json`, `geas-10-02-mastery.json`, `geas-10-03-mastery.json`)**:
  - Rebalanced 75 questions from 92–100% Option A bias to an even ~25% distribution ($A:6, B:6, C:6, D:7$ per 25-item set) with zero consecutive identical answer streaks.
- **Phase 1 CI Audit Verification**:
  - `geas-10-01.json`: **✅ PASS**
  - `geas-10-02.json`: **✅ PASS**
  - `geas-10-03.json`: **✅ PASS**
  - `geas-10-01-mastery.json`: **✅ PASS** `[A:6 B:6 C:6 D:7]`
  - `geas-10-02-mastery.json`: **✅ PASS** `[A:6 B:6 C:6 D:7]`
  - `geas-10-03-mastery.json`: **✅ PASS** `[A:6 B:7 C:6 D:6]`
- **Production Build Verified**: `npm run build` compiled with 0 errors across all 28 Next.js routes.








